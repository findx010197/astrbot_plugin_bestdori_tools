# This directory stores downloaded font files for embedding in HTML
# Font files will be downloaded automatically on first run if Chinese font support is not detected
# Supported formats: .woff2 (preferred), .otf, .ttf
